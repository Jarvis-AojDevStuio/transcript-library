Run `/prime` first to orient yourself.

Then read `.claude/hooks/setup.maintenance.log` and analyze the maintenance results:

1. Check dependency update status
2. Check type checking results
3. Check lint results
4. Write a summary to `app_docs/maintenance_results.md` with:
   - Update status
   - Type check pass/fail with details
   - Lint pass/fail with details
   - Recommendations for fixing any issues
5. Report the results to the user
